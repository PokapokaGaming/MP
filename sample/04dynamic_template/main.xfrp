# Static party definition
party main
leader = c1
periodic(1000)
newnode (c1) = client()

# Template for dynamically creating new clients
party_template ClientTemplate
leader = client
periodic(500)
newnode (client) = client()
