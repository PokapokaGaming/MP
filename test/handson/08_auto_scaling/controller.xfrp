module controller
in load_metric: Int
out command: Int

node init[0] command: Int = 
  if load_metric > 10 then
    if (command@last == 0) then 1 else 0
  else
    if load_metric < 5 then
      if (command@last == 0) then 2 else 0
    else
      0
